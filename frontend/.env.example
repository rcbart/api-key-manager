# Base URL of the backend API. Leave unset in local dev -- Vite's dev server
# proxies /api to http://localhost:8080 (see vite.config.ts). Set this for a
# production build pointed at a deployed backend.
VITE_API_BASE_URL=
